# Agent Handoff: Shadow Assassin Engine Check

- Date: 2026-07-01
- Agent/thread: Codex desktop
- Scope: shadow assassin combat behavior and equipment grind branch visibility
- Status: complete

## User Intent

The user reported that the assassin engine appeared completely inactive: no blink, no backline dive, and no target cutting.

## Completed

- Verified the shared `combat-sim` path with an explicit shadow assassin kit:
  - `shadowBurstAmbush / throatCut / shadowMomentum / midnightBloom`.
  - The assassin blinked to the enemy mage at 1.84s.
  - The assassin locked the mage as `assassinFocus`.
  - After killing the mage, the assassin reset/blinked to the enemy priest.
- Found a real fallback-engine gap in `battle_view`:
  - the legacy local battle engine exposed `createSkillLibrary()` but did not provide `shadowStepStrike`, `blinkBacklineStrike`, or `backlineLowestEnemy`;
  - therefore pages not using `GAME_COMBAT_SIM` could cast the skill with no blink effect.
- Added those fallback battle-view APIs:
  - backline target selection;
  - blink movement;
  - forced target lock;
  - hidden/guard timers;
  - mark application and movement/status signals.
- Clarified equipment grind roster generation:
  - kept the existing poison assassin branch;
  - added a distinct generated `暗影刺客` branch that still uses role `assassin` but equips the shadow kit;
  - UI summaries now use the hero's actual equipped skills instead of only `roleKits.assassin`, so a shadow assassin is visibly different from a poison assassin.

## Files Changed

- `projects/western_fantasy_continent/battle_view/battle-view.js`
  - Added missing fallback combat APIs for shadow assassin blink/target behavior.
- `projects/western_fantasy_continent/equipment_grind_simulator/equipment-grind-simulator.js`
  - Added `暗影刺客` as a roster-generation branch using the shadow kit and fixed hero summaries to reflect actual equipped skills.

## Validation

- `node -c projects/western_fantasy_continent/battle_view/battle-view.js`: passed.
- `node -c projects/western_fantasy_continent/equipment_grind_simulator/equipment-grind-simulator.js`: passed.
- Node simulation against `combat-sim` confirmed:
  - first shadow movement signal at 1.84s;
  - movement tags include `movement`, `blink`, `backline`, `shadowStep`, `hidden`;
  - first blink target is enemy mage;
  - target lock signals include `assassinFocus`;
  - kill reset movement target is enemy priest.

## Current State

The shared combat engine is functioning for shadow assassin. The user-visible confusion came from two places:

- the default `roleKits.assassin` is still poison assassin, so generic "刺客" in generated rosters does not blink;
- the legacy `battle_view` fallback did not implement shadow movement APIs.

The equipment grind simulator can now generate a clear `暗影刺客` branch. Existing saved rosters in `localStorage` are not forcibly migrated; use reset/reroll to generate the new branch.

## Unresolved

- Manual browser click-through was not completed in this pass.
- There is still no explicit in-UI skill/branch selector; the branch is generated by roster reroll. If the user wants deterministic testing, add a team-page branch switch next.

## Recommended Next Step

Open the equipment grind simulator, reroll until `暗影刺客` appears, and watch whether it blinks in the live battle. If manual control is needed, start in `equipment-grind-simulator.js` and add a small branch toggle for assassin heroes.
