# Agent Handoff: 三标尺换人后的换装重算小案例

- Date: 2026-07-19
- Agent/thread: root
- Scope: 用最小确定性案例检查三标尺配比换人后再换装是否受旧综合强度影响
- Status: complete

## User Intent

用户要求不要跑完整模拟，只用一个小案例验证上轮指出的边缘问题：初始换人已经按输出、保护、增益配比计算，但之后换装时是否仍会被旧通用强度路径影响。

## Completed

- 新增一个独立公式级审计，不调用Agent、不跑多玩家、不跑两章地图。
- 构造两个完全相同的换人案例：
  - 当前需求都是 `输出2 / 保护7 / 增益1`；
  - 三条能力差、证据数、关卡、初始战斗基线和装备提升都相同；
  - 只让内部旧综合强度分别为 `2` 和 `8`。
- 两个案例先走正式配比冻结，再走同一套 `1.25` 倍装备重算。
- 审计只记录现状，没有修改正式算法。

## Files Changed

- `projects/western_fantasy_continent/experiments/player_agent_api_loop_v1/audit-capability-mix-equipment-rebase.js`：最小换人配比与换装重算隔离案例。

## Validation

- JavaScript语法：PASS。
- 小案例执行：`DIAGNOSIS_CONFIRMED`。
- 初始换人阶段：
  - 两个案例都保留 `2/7/1` 配比；
  - 加权能力差都为 `6.2`；
  - 初始预测分都为 `0.344`。
- 同样增加25%装备后：
  - 旧综合强度为 `2` 时，预测分从 `0.344` 变为 `0.404`；
  - 旧综合强度为 `8` 时，预测分从 `0.344` 变为 `0.584`；
  - 两者出现 `0.18` 的无关差异。
- 配比记录在换装后仍然存在，A曲线没有变化。
- `independent_review`：未运行；这是公式接线审计，不是玩法或真实Agent轨迹。

## Current State

问题被精确定位为：

```text
初始换人预期 = 三标尺临时配比                 正确
随后换装增量 = 旧综合强度 × 装备倍率变化      仍有旧路径
最终换装后预期 = 初始配比预期 + 上述旧路径增量  会被污染
```

所以系统不是“换装后重新只看输出”，也没有丢失Agent的配比；真正的问题是，换装增量大小仍取决于一个已经不该代表该次关卡需求的旧综合强度。

## Unresolved

- 尚未决定三维装备怎样作用于输出、保护和增益，因此本轮不直接改算法。
- 在修复前，“先换人再换装”的最终预期可能因旧综合强度不同而产生无关偏差；本例偏差为 `0.18` 战斗表现分。

## Recommended Next Step

先决定装备对三套能力的可见作用口径。最小安全方向是：冻结换人配比后，换装也先形成输出、保护、增益三条装备变化，再用同一配比重算；缺少维度证据时保持未知。不要直接把旧综合强度替换成另一个新的永久总分。
