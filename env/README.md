# Env

This folder is for local API keys and runtime configuration.

Create a local `env/.env` file from `.env.example` and fill in real keys there. The real `.env` file is ignored by Git.

Do not commit real API keys.

