# Battle Autochess Prototype

Open `index.html` in a browser to run the prototype.

This module is a first playable combat base for the western fantasy continent project. It keeps the implementation local and dependency-free so the rules can evolve quickly.

