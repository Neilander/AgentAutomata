# Map Cognition Iteration: V2.2 Subagent Review Gate

- Time: 2026-07-09 20:53 local heartbeat
- Basis: subagent reviews for `2026-07-09_2047_v2.2_m6-blue-quality-signal-candidate.md`
- Status: candidate accepted only behind M5/user-playtest gate

## Review Summary

Two read-only agents reviewed the V2.2 candidate. Both agreed:

```text
V2.2 is reasonable as a candidate.
V2.2 should not be implemented before user playtest validates the first loop and M5 role proof.
```

## Confirmed Safe Shape

M6 may later introduce:

```text
蓝装通常值得检查
首次击破 / 里程碑节点可能给更高质量奖励
```

M6 must not introduce:

```text
蓝装必需
蓝装总比白装强
稀有度 = 词条 / 等级公式
需要配装优化
```

## Lock-Key Interpretation

M6 should not be a new hard lock.

```text
lock:
  none / normal milestone challenge only

key:
  none; blue is not the key

treasure:
  1 蓝装 + normal small rewards

knowledge update:
  milestone rewards are worth checking
```

## Gate Condition

Do not implement V2.2 unless at least one of these is true:

```text
User playtest says M5 role proof is understandable.
M5 gets real combat/team wiring that makes role process visible.
```

If M5 remains only text and the user has not validated it:

```text
Implementing M6 blue reward is serious risk.
It may pull attention back to equipment/power and make role learning disappear.
```

## Failure Attribution Rule For M6

If M6 fails after M5 was perceived:

```text
primary attribution:
  role / battle process usage insufficient
secondary attribution:
  equipment/power can still improve
```

If M5 was not perceived:

```text
primary attribution:
  power/equipment insufficient
risk:
  blue reinforces old equipment-only thinking
```

## Severity

As candidate:

```text
minor
```

If implemented before M5/user-playtest gate:

```text
serious
```

## Next Step

Next heartbeat should not implement V2.2 by default.

Priority remains:

```text
Get user playtest feedback on:
  - Prison-first
  - first-fail feel
  - Camp-as-key
  - M5 role proof clarity
```

If no user feedback exists, either improve M5 proof design or wait; do not add M6 reward signaling yet.
