# Map Cognition Iteration: V1.2 + V2.1 Map-Lab Implementation Plan

- Time: 2026-07-09 21:27 UTC heartbeat
- Basis: V1.2 accepted for implementation-test; V2.1 narrow rarity signal
- Status: draft pending implementation/认知 review merge

## This Iteration's Goal

Translate the accepted design slice into an implementation-facing plan for `map_progression_lab`, without writing code yet.

Target:

```text
V1.2:
  Prison A is psychologically first.
  Camp A is preparation before Prison failure and highlighted key after failure.
  M5/M6 prove the rescued character through process metrics.

V2.1:
  blue rarity appears only as reward quality signal.
```

Do not introduce:

```text
equipment level system teaching
affix optimization
field effects
purple rewards
build-specific gear choice
```

## Current Implementation Snapshot

Observed current map-lab structure:

```text
r1 has no entrance gate.
r1 main 1-10 exists.
r1_bandit requires r1_main_4.
r1_bandit reward currently says 1 紫装, 2 蓝装.
r1_prison requires r1_main_5.
r1_prison reward gives 林地游侠.
r1_boss requires r1_main_10.
```

Main conflicts with V1.2/V2.1:

```text
Prison appears after Camp, not psychologically first.
Camp reward uses purple too early.
Camp is a normal branch reward, not stateful preparation/key after Prison failure.
No explicit failure-memory state for Prison A.
No role process-proof node is defined after Prison success.
Rarity signal is too strong and too early because of purple reward.
```

## Target Cognition Model

Before M4:

```text
concepts:
  - 关卡
  - 战斗
  - 装备
  - 战力

knowledge:
  - 装备能提高战力
  - 普通装备是小幅、可叠加提升

behaviors:
  - 挑战关卡
  - 换装备

failure_memories:
  - none
```

After M4:

```text
concepts:
  + 监狱
  + 角色
  + 军械营地
  + 蓝装 as weak reward signal

knowledge:
  + 监狱里有可救角色
  + 军械营地也许有攻坚装备

behaviors:
  + 挑战 Prison A
  + 查看/准备 Camp A
```

After Prison A failure:

```text
failure_memories:
  - Prison A:
      baseline_power: current team power
      attribution: 战力不足 / 装备还不够
      wake: power >= baseline * 1.3

knowledge:
  + Camp A is likely equipment key for Prison A
```

## Implementation Plan

### 1. Add Map State For Prison Failure

State field:

```text
state.flags.r1PrisonFailed = true | false
```

or equivalent:

```text
state.failures.r1_prison = {
  baselinePower,
  failedAt
}
```

Minimum first pass can use boolean if power is not implemented in map-lab yet.

Purpose:

```text
Before r1PrisonFailed:
  Camp A is preparation clue.
After r1PrisonFailed:
  Camp A becomes highlighted equipment key.
```

### 2. Change First-Region Unlock Order

Current:

```text
Bandit requires main_4.
Prison requires main_5.
```

Target:

```text
After r1_main_4:
  reveal both r1_prison and r1_bandit.
  select/focus r1_prison by default.
```

Prison A:

```text
requires: r1_main_4
salience: main desire point
initial status: low-cost attempt
```

Camp A:

```text
requires: r1_main_4
salience before r1PrisonFailed: weak/medium
salience after r1PrisonFailed: strong
```

Fallback V1.3:

```text
If playtest shows Camp-first behavior:
  r1_bandit remains visible after main_4 but cannot be challenged until r1PrisonFailed.
```

### 3. Focus / Selection Behavior

When `r1_main_4` is cleared:

```text
selectedId = "r1_prison"
mapFocus = r1_prison
```

Rationale:

```text
The first camera/selection event establishes Prison A as the main desire point.
```

Do not:

```text
default-select r1_bandit
flash r1_bandit reward
open reward preview for Camp A first
```

### 4. Node Text And Rewards

#### r1_prison

Before clear:

```text
name: 旧塔监狱
desc: 里面关着一名可救出的角色。先试着救人。
reward: 林地游侠
```

On first failure:

```text
message: 战力还不够，也许附近的军械能帮助攻坚。
```

Do not mention:

```text
阵容不足
需要蓝装
需要稀有度
```

#### r1_bandit

Before Prison failure:

```text
name: 旧塔军械营地
desc: 附近的军械营地，也许有攻坚装备。
reward preview: 高等级白装 + 1 蓝装
salience: weaker than Prison
```

After Prison failure:

```text
desc: 旧塔军械也许能帮你重新攻进监狱。
reward preview: 高等级白装 + 1 蓝装
salience: highlighted
```

Reward change:

```text
from: 1 紫装 + 2 蓝装
to: 2 高等级白装 + 1 蓝装
```

Reason:

```text
Purple is too early and steals attention.
Blue is enough as V2.1 reward-quality signal.
```

### 5. Prison Fight Result For Lab Prototype

Current map-lab challenge is auto-win. For implementation-test, introduce a controlled first-fail behavior only for `r1_prison`:

```text
If r1_prison challenged before r1PrisonFailed and before r1_bandit clear:
  mark r1PrisonFailed = true
  do not clear r1_prison
  log failure message
  highlight r1_bandit
```

After `r1_bandit` clear:

```text
r1_prison can auto-win / clear.
```

Fallback:

```text
If forced failure feels too artificial, make it "险些失败/撤退" in prototype text.
```

### 6. M5/M6 Role Process Proof

Because current map-lab may not have real team composition rewards wired into map nodes, first implementation can record intent in node text/log:

After Prison A success:

```text
reward: 林地游侠
behavior: 上阵新角色
```

Next main node after Prison success:

```text
M5 or M6 should describe a visible role process metric:
  林地游侠提前击杀后排哨兵
  清场时间缩短
```

Do not use rarity on this node.

Implementation-light option:

```text
Add reward log / node desc text:
  上阵林地游侠后，后排威胁会更早消失。
```

Later implementation option:

```text
Connect actual rescued character to team and battle simulation.
```

### 7. V2.1 Rarity Signal Placement

Allowed:

```text
r1_bandit reward preview: 高等级白装 + 1 蓝装
r1_bandit clear log: 获得一件蓝装，它通常更值得检查。
M6 first-clear reward: 1 蓝装, only if role proof has already happened.
```

Forbidden:

```text
purple rewards in first region before Boss
affix text
blue as required key
gear optimization tips
rarity teaching on role-proof node
```

## Lock-Key Chain In Implementation

```text
M4 clear -> reveals Prison A desire and Camp A preparation clue
Prison A first attempt -> failure memory: power/equipment insufficient
Camp A -> equipment package key, +30%-50% intended gain
Prison A retry -> character treasure
M5/M6 -> role process proof and first process wall
```

## Playtest / Review Feedback

### Agent A: Implementation Conflict Review

Timed out and was closed before returning output.

### Agent B: Cognition Simulation Review

Timed out and was closed before returning output.

## Macro Judgment

Skill-model problem:

```text
none expected
```

Design problem:

```text
implementation must not make Camp A visually or procedurally first.
```

Review-method problem:

```text
subagent review did not complete within this heartbeat; this plan remains unreviewed by agents.
```

## Rollback Decision

Current severe problem estimate:

```text
minor if implemented with Prison-first focus
serious if Camp remains more attractive / earlier than Prison
```

Review status:

```text
implementation plan drafted but not independently reviewed.
next heartbeat should review this plan before code changes.
```

Fallback:

```text
V1.3: Camp A preview-only until Prison A first failure.
```

## Skill Update Decision

No skill update planned this pass.

## Next Iteration Suggestion

If implementation plan passes review:

```text
Apply the smallest map-lab code change to first region only.
Validate with browser or user playtest.
```

If it fails:

```text
Use V1.3 hard gate: Prison failure unlocks Camp challenge.
```

Immediate next step:

```text
Review this implementation plan first. Do not implement until the review confirms the plan preserves Prison-first cognition.
```
