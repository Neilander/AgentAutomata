# Test results

Evidence validator checks required files, unique state/seed markers, and that machine records are newline-delimited JSON. It does not read state_attempt_2026082508_v8 or private audit files.
