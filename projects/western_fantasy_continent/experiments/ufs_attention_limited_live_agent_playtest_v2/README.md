# UFS 受153项概率注意限制的现场试玩 V2

本目录记录一次不可重试的现场试玩。玩家每一步只看到41项注意预算裁剪出的环境，并只提交一个当前操作。

实验协议见 `PROTOCOL.md`；逐步想法写入 `thought-log.jsonl`，裁剪机器视图、单步选择与人工整理的时序保留在本目录，结果见 `RESULTS.md`。

`runtime/`是CLI状态目录，内含玩家协议禁止读取的宿主checkpoint，因此生成文件不纳入实验产物；封卷后的主Agent可按自己的审计协议处理它。这不是OS级隔离。

唯一Attempt在研究房效果边界因没有可用操作口停止，没有重试。
