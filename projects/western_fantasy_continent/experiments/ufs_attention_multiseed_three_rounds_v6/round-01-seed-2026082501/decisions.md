# Round 01 决策流水

## 00 start 前

- 已注意事实：尚无局面；只知道公开合同要求从全新 state 目录开始，实际种子应由响应回显。
- 当前目标：创建本局唯一 attempt，取得首个裁剪视图。
- 候选取舍：没有游戏内动作候选；仅可调用 `start`。
- 选择/预想：以 `UFS_ATTENTION_SEED=2026082501` 调用 `start`；预想返回当前 noticed view、pending 和可用操作。

## 01 place_die 前

- 已注意事实：阶段为 dice，能量2；可见未放骰为灰3、灰4、白5、白1；`A-upper-energy` 是两格 `A-r2-c4/c5`，修正-3；这些格和房间均被注意到。C3、C4有紫船，而C4天空起点带向C5箭头。
- 当前目标：先占能源双格的一格，保留白5作为第二格，争取形成较高房间值并补能源。
- 候选取舍：灰4→C4与白5→C5都能做能源配对；先用灰4避免提前消耗会重投的白骰，也比灰3更适合当前高值组合。防空单格虽然安全，但暂不产资源。
- 选择/预想：灰4→`A-r2-c4`；预想同列紫船下降4并可能沿已注意到的C4→C5箭头转列，能源房暂未完整，然后返回下一次放骰选择或因未注意终点而停止。

## 02 place_die 前

- 已注意事实：操作成功并再次等待放骰；灰4现为已放。当前注意中新出现灰2和C3通道，但能源房的另一格C5没有出现在当前 baseCells，已不能按公开合同提交它；当前只见紫船C1，而先前C4船的结果未被当前视图确认。
- 当前目标：在可见合法格中继续放置，同时避免凭记忆向当前未暴露的C5补提交。
- 候选取舍：白5→`A-r1-c5`可做防空但会进入随机门；灰3→`A-r2-c3`是当前明确可见的普通通道，预计无房间产出但可推进C3飞船。先走灰骰，保留白骰随机边界。
- 选择/预想：灰3→`A-r2-c3`；预想骰子占位、C3飞船下降3、通道无普通房间产出，然后仍返回放骰选择。

## 03 place_die 前

- 已注意事实：灰3已放，紫船2被看见在C2第3行；当前能源房、其C5格、白5均重新进入注意，且C5当前未见占用记录；可用操作仍仅放骰。
- 当前目标：补齐先前开始的两格能源房，恢复资源路线。
- 候选取舍：白5→C5可与已执行的灰4→C4构成双格；灰2→C1战斗机房需要耗能且当前只看见C2紫船，收益不确定；防空白骰更保守但放弃能源组合。
- 选择/预想：白5→`A-r2-c5`；预想能源房完整并记录房间值，随后白骰进入外部随机边界，必须调用 CLI `random`。

## 04 random 前

- 已注意事实：状态为 `random/waiting_for_actual_reroll`；pending明确要求重投 `r1-gray-0` 与 `r1-white-4`，唯一可用操作是 `submit_random_observation`。当前视图确认灰2和白1尚未放。
- 当前目标：取得真实外部骰值并恢复回合，绝不自行编造结果。
- 候选取舍：公开合同只允许 CLI `random`；无其他合法动作。
- 选择/预想：调用 `random`；预想两个未放骰获得外部结果，返回下一次放置选择。

## 05 place_die 前

- 已注意事实：外部结果把未放骰变为灰5、白2；当前明确看见`A-r2-c2`研究房（耗能2），能量仍2，研究0；C2有两架紫船（第0、3行），且第1行C2箭头指向C1。
- 当前目标：把灰5用于可见研究房，建立本局研究推进候选；能源双格预计能在房间阶段补资源。
- 候选取舍：灰5→研究能形成高研究预算但需支付当前全部能量；灰5→C1战斗机修正-1且耗能1，当前所见紫船都在C2，击毁收益存疑；路径能源格解锁4而挖掘0，不选择可能锁定的格。
- 选择/预想：灰5→`A-r2-c2`；预想C2两船受下降影响并可能经已注意箭头转到C1，研究房形成待结算预算，随后只剩白2。

## 06 place_die 前

- 已注意事实：灰5已放；最后可见未放骰是白2。当前C1有紫船0在第0行，`A-r2-c1`战斗机房修正-1、耗能1，当前能量2；`A-r1-c1`防空也可见。母舰已被观察到第0行，说明上步的落点效果发生了。
- 当前目标：用最后骰形成可结算战斗候选，同时保留至少1能量；避免只做无资源的防空。
- 候选取舍：白2→战斗机房得到推测值1，可能至少能针对C1近端紫船；白2→防空仅减移动且此时C1船就在起点，收益较弱。
- 选择/预想：白2→`A-r2-c1`；预想C1船下降2、战斗房待结算，随后骰子耗尽进入房间阶段；若没有未放骰则不应再要求重投。

## 07 resolve_room 前

- 已注意事实：现为rooms阶段；五个placement全部可见且未结算。能源双格明确由灰4和白5占据，房间修正-3；研究灰5、通道灰3、战斗白2也可见。当前能量2，可用操作包含`resolve_room`。
- 当前目标：先结算零成本能源房，为后续研究耗能2与战斗耗能1提供余量。
- 候选取舍：先研究会耗尽当前能量，使战斗无法支付；先能源预计净增6（4+5-3）并消除资源约束。通道无需收益，稍后跳过。
- 选择/预想：以`pay:true`结算`A-upper-energy`；预想能源增加，仍停在房间行动。

## 08 resolve_room 前

- 已注意事实：能源现为7，能源placement已resolved；研究灰5仍未结算且研究房耗能2；战斗白2与通道灰3仍未结算。可用操作保持不变。
- 当前目标：支付研究房并取得公开的最大推进范围，再按pending做实际推进选择。
- 候选取舍：此时能量足以同时覆盖研究2与战斗1；先研究不会阻断其他计划。直接end_rooms会浪费已建立的研究房。
- 选择/预想：以`pay:true`结算`A-upper-research`；预想能量降至5并出现`choose_research_advance`操作及公开预算/最大步数。

## 09 choose_research_advance 前

- 已注意事实：pending公开研究预算5、连续成本从3、1开始，最大推进2步；唯一可用操作是`choose_research_advance`，当前研究0。
- 当前目标：在预算内最大化研究推进。
- 候选取舍：1步花3，2步共花4且不超过预算5；没有看到保留房间预算的其他用途，故选2步。
- 选择/预想：`A-upper-research`推进2步；预想研究到2，研究placement结算完成，回到普通房间行动。

## 10 resolve_room 前

- 已注意事实：研究现为2；剩余未结算placement为通道灰3与战斗白2。战斗房修正-1、耗能1，能量5；紫船0在C1第2行，而当前注意到的C1第3行爆炸标记为2。
- 当前目标：尝试用战斗房处理当前C1威胁，之后跳过无产出的通道工人。
- 候选取舍：房间值推测为1（白2-1），可能不足以处理爆炸2目标；但支付只耗1且本回合无更多能源用途。直接跳过会确定放弃潜在击毁。
- 选择/预想：以`pay:true`结算`A-upper-fighter`；预想能量降至4，可能因房间值不足而没有合格目标，但placement应被结算。

## 11 skip_worker 前

- 已注意事实：能量降至4，战斗placement已resolved，紫船0仍在C1第2行；唯一未结算的是`r1-gray-1@A-r2-c3`，其roomType为tunnel、非挖掘候选。可用操作包含`skip_worker`。
- 当前目标：原子地处理最后一个无产出工人，再结束房间阶段。
- 候选取舍：`excavate`不适用于`excavationCandidate:false`；`resolve_room`对普通通道没有收益依据；跳过最符合当前公开事实。
- 选择/预想：跳过placement `r1-gray-1@A-r2-c3`；预想其标记resolved并仍停在房间行动。

## 12 end_rooms 前

- 已注意事实：placements当前为空，阶段仍为rooms，可用操作包含`end_rooms`；能量4、研究2。
- 当前目标：结束已处理完的房间阶段，推进母舰/生成自动链。
- 候选取舍：没有可见未结算工人，继续resolve/skip没有对象；只应结束房间。
- 选择/预想：提交`end_rooms`；预想进入母舰下降及所在行行动的自动后果，随后可能出现生成选择或直接完成。

## 13 choose_spawn 前

- 已注意事实：母舰阶段、母舰第2行，outcome为空；等待生成的是紫船0，pending仅给候选`DP-C1`和`DP-C3`。当前场上C2有两船、C4和C5各一，C1/C3为空；唯一可用操作`choose_spawn`。
- 当前目标：把等待紫船放到空列并完成回合。
- 候选取舍：C1与C3都满足当前候选；C1刚有船因母舰行行动回到等待队列，且当前可见C1近端爆炸格较密；选择C3以分散列压力。
- 选择/预想：紫船0→`DP-C3`；预想生成完成并抵达`complete/new_round`，若仍有生成边界则继续按新pending判断。
