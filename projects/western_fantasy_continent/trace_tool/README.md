# Trace Tool

Open `index.html` to trace the first battle character reference.

The tool creates reusable character data:

- skeleton joints
- bone links
- part polygons
- exportable JSON

The default reference image is loaded from `picref/战斗小人参考.png`.

